# `electron` is a devDependency for its type declarations only (the plugin
# runs inside Obsidian's own Electron). Its install script would otherwise
# download a 100 MB binary nobody here runs.
electron_skip_binary_download=1
